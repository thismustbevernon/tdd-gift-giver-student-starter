class GiftExchage {
 
  static async pairs(names) {
    if (names.length/2 !== 0) {
      throw "number of names must not be odd!";
    } else {
        arr1 = nums


    return generatedPairs
    }
  }

  static async traditional(names) {}
}

module.exports = GiftExchage;
